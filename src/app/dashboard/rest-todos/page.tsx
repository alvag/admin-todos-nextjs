import { FC } from 'react';

interface RestTodosProps {
}

const RestTodosPage: FC<RestTodosProps> = ( {} ) => {
    return (
        <div>RestTodos Page</div>
    );
};

export default RestTodosPage;
